import numpy as np
from tooling import halo_update
from core import convection, diffusion

def main():
    # Δx = L_x / N
    # Δy = Ly / N
    sx = np.linspace(0, Lx, N)
    sy = np.linspace(0, Ly, N)

    cx = sx - d
    cy = sy -

if __name__ == "__main__":
    main()
